apiVersion: v1
kind: Service
metadata:
  name: orchestrator-svc
  namespace: default
  labels:
    app: orchestrator
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: orchestrator
    tier: backend