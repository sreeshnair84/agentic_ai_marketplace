apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: default
spec:
  selector:
    app: rag
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
  sessionAffinity: None